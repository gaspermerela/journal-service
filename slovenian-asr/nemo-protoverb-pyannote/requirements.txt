# RunPod Handler Dependencies
# These are installed in the Docker image

# NeMo ASR toolkit (required by PROTOVERB model)
nemo_toolkit[asr]==1.21.0

# RunPod serverless SDK
runpod>=1.6.0

# Audio file handling
soundfile>=0.12.1

# pyannote speaker diarization (replaces NeMo ClusteringDiarizer)
# MIT license, requires HuggingFace token for model download
pyannote.audio==3.1.1

# NFA (Forced Alignment) for word-level timestamps
# Uses aligner_utils.py copied from NeMo 2.x (compatible with 1.21.0)
# Provides Viterbi-optimal alignment (~95% accuracy)
# No additional dependencies needed - reuses PROTOVERB ASR model
